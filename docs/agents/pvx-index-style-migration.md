# PVX 全站风格统一改造进度

## 1. 文档定位

本文只记录 PVX 模块改造范围、实施顺序、业务保护要求、验收状态和后续 TODO。

视觉风格、页面布局、设计令牌、基础组件外观、响应式规则和样式隔离规范统一维护在：

- `docs/design/pvx-visual-style-guide.md`

开始任何页面改造前必须先阅读该设计规范。本文不再重复维护视觉细节。

## 2. 改造边界

改造只调整表现层和必要的公共结构，不得改变：

- 接口地址、请求参数和返回数据语义；
- 登录、权限、作者和管理员判断；
- 购买、支付轮询、附件获取和下载流程；
- 路由名称、跳转行为、统计与埋点；
- 第三方业务组件自身的数据解析和国际化逻辑。

未进入当前阶段的页面不得提前受到样式影响。

## 3. 已验收模块

以下模块已经完成改造和用户验收，属于回归保护范围：

| 模块 | 主要路径 | 已覆盖内容 |
| --- | --- | --- |
| PVX 首页 | `/pvx/` | 分类面板、业务入口、插图和响应式布局 |
| 捏脸数据 | `/face/` | 列表、详情、解析页、移动端与多语言 |
| 体型数据 | `/body/` | 列表、详情、解析页、移动端与多语言 |
| 家园蓝图 | `/homeland/` | 首页、家园信息、升级数据、地图及子页面 |

新增公共能力时必须回归这些模块，不能为了后续页面破坏已验收表现或业务功能。

## 4. 已完成公共能力收敛

### 4.1 基础设计组件

已建立：

- `PvxPageShell`
- `PvxSurface`
- `PvxSectionHeader`
- `PvxToolbar`
- `PvxActionButton`
- `PvxEmptyState`

组件用途和外观规则记录在视觉设计规范中。

### 4.2 Face / Body 公共能力

已收敛：

- `Tabs.vue`：列表筛选与搜索工具栏；
- `ListItem.vue`：列表卡片；
- `SingleNavigation.vue`：详情页返回和导航；
- `SingleHeader.vue`：详情页作者与基础信息；
- `SinglePaySection.vue`：购买、附件与下载操作区；
- `SingleCarousel.vue`：详情预览与全屏单图切换；
- `SingleRandomList.vue`：相关推荐；
- `ParsePage.vue`：Face / Body 解析页公共外壳；
- `pc-list-mixin.js`：列表请求、数量计算和筛选逻辑；
- `pc-single-mixin.js`：详情、购买、支付轮询、附件与下载公共逻辑。

Face / Body 页面只保留真实业务差异，例如接口方法、数据解析器、类型标识、文案键和个别模块操作。

第三方 `@jx3box/jx3box-facedat` 组件直接使用，不复制其内部国际化，也不增加重复包装。

## 5. 后续改造 TODO

后续从信息查询类模块开始，每个模块单独改造、单独验收。

### 5.1 信息查询类（当前阶段）

- [ ] 活动告示每日速览 `/pvg/gonggao/daily`
- [ ] 活动告示日历及详情 `/pvg/gonggao/calendar`
- [ ] 活动告示开服状态 `/pvg/gonggao/server`
- [ ] 考试大全 `/exam`
- [ ] 技艺助手 `/pvg/manufacture`
- [ ] 价格趋势 `/pvg/price`
- [ ] 信息查询类模块完成后收敛公共列表、筛选、工具栏和空状态

活动告示三页已经完成改造和技术回归，当前等待用户最终验收，因此暂不勾选上述 TODO。

本轮已完成：

- 三个入口统一使用现代页面外壳、标题、卡片和 Sticky 分段导航，保留原路由与子页面切换行为；
- 页面级标题图标统一为 `44px` 容器和 `24px` 图形，并使用与家园蓝图一致的深色渐变、暖色描边和克制 Hero 背景；
- 每日速览重组公告、服务器、家园声望、名士和抓马信息，服务器状态改为方形信息卡，抓马条目单行完整展示并统一字号、对齐与状态层级；
- 活动日历采用七列月历与右侧日期详情布局，导航和详情面板分别固定在合理位置，小屏自动回到普通文档流；
- 开服状态按大区使用响应式方形卡片展示，保留登录、收藏和服务器状态业务逻辑；
- 日历新增、编辑、删除、复审、图片与参考资料等管理能力保持原逻辑，并修复弹窗关闭、空数据和异常数据兼容问题；
- 补齐简体中文、繁体中文、英文和越南文界面文案；游戏专名、服务器名称和接口内容继续使用源数据，不做错误翻译；
- 页面样式分别隔离在 `gonggao-daily-theme.less`、`gonggao-calendar-theme.less` 和 `gonggao-server-theme.less` 中，没有影响未改造入口。

本轮技术检查已通过：`npm run lint`、`npm run verify:legacy-js`、`npm run build:dev` 和 `git diff --check`。浏览器已回归三个入口切换、日历月份切换、日历弹窗开关及服务器卡片加载。

考试大全当前已经完成 Web 端第一轮改造和多轮视觉调整，等待用户最终验收，因此 `/exam` 主 TODO 暂不勾选。

本轮考试大全已完成：

- 统一 `/exam`、`/exam/:type`、`/exam/paper/:id` 和 `/exam/question/:id` 的现代页面外壳；“我要出题”等外链继续使用原入口，小程序/App 专用考试页不进行视觉重构；
- 页面级标题图标统一为 `44px` 容器和 `24px` 图形，Hero 背景与家园蓝图、活动告示保持同一视觉层级；
- 首页四个业务入口、搜索、筛选、发布入口、结果统计、分页、加载和空状态已经按现有设计组件重组，筛选按钮支持明确手型、打开状态和克制 Hover；
- 科举题库改为响应式结果卡片，搜索命中词保持轻量高亮，“答案”标签使用普通正文层级，答案值使用重点色和较高字重，同排卡片通过 Flex 自然对齐答案区域；
- 创作题库桌面端继续使用表格，行操作改为 Element Plus `primary plain` 按钮并保留完整 Hover；平板和手机改为响应式卡片，不影响原题目跳转；
- 模拟试卷列表使用 `auto-fill + minmax(320px, 1fr)`，大屏减少过密列数，移动端保持单列；
- `single_card.vue` 已统一题目编号、题型、标签、选项、作答状态和解析区域，并修复 Element Plus 属性警告、富文本图片溢出和移动端选项布局；
- 试卷详情重组导航、试卷信息、难度、标签、说明、作者、题目、提交、感谢和评论区域；移除重复的“试卷题目”标题，说明图标在重点色背景上使用反白显示；
- `paper_title.vue` 已收敛试卷标题信息和得分状态，并兼容空标签、空说明、零分及异常数组数据；
- 题目详情沿用同一题目卡和社区区域，保留登录、提交、判分、编辑、统计、感谢与评论逻辑；
- 考试首页、试卷详情和题目详情在模块根节点内解除 `PvxPageShell` 的 `1440px` 上限，大屏填满可用宽度，同时保留公共左右安全边距；
- 搜索图标与输入文字使用同一垂直中心，筛选浮层按钮统一为“关闭 / 重置”，新增界面文案已补充简体中文、繁体中文、英文和越南文。

考试模块样式隔离在 `exam-shell-theme.less`、`exam-index-theme.less`、`exam-paper-detail-theme.less` 和 `exam-question-theme.less` 中。已运行目标文件 ESLint、`npm run verify:legacy-js`、`npm run build:dev` 与 `git diff --check`，并使用 Chrome 回归考试首页、试卷详情、桌面/平板/手机布局及单选交互；未执行真实试卷提交，避免产生业务数据。

技艺助手已经完成 Web 与移动端第一轮改造，当前等待用户统一验收，因此 `/pvg/manufacture` TODO 暂不勾选。

本轮技艺助手已完成：

- Web 端统一使用 `PvxPageShell`、`PvxSurface`、`PvxSectionHeader`、`PvxToolbar` 和 `PvxEmptyState`，生活技艺分类、配方、成本计算器与我的清单形成明确主次布局；
- Web 端“我的清单”由常驻侧栏改为页头入口与右侧 Drawer，抽屉继续复用原清单组件，保留查看、批量选择、删除与合并逻辑；查看或合并后自动回到成本计算器；
- 清单批量操作已按 Vue 3 修复复选框绑定与事件冒泡，补充已选数量、选中态、全选、取消、历史清单筛选、操作禁用、加载状态及接口成功/失败反馈；“合并”文案明确为加入当前成本计算器且不删除原清单；
- 页面级标题继续使用 `44px` 容器与 `24px` 图形，区服选择提升至 Hero 右侧，分类和搜索组合为 Sticky 工具栏；
- 桌面大屏使用配方主区与右侧工作区双栏，窄桌面和平板自动折为单栏；移动端保留原独立业务页面，但改为浅色卡片、两列技艺入口、单列配方与固定底部工具栏；
- Web 配方列表与详情面板通过 `ResizeObserver` 同步可视高度，左侧列表独立滚动；物品说明 Popper 和价格编辑 Popover 增加视口上限与移动端重排，长说明、长物品名和窄屏输入不再被裁切；
- 手机端增加首页 Hero、配方列表/详情/清单详情上下文导航和确定性返回路径；分类、配方、返回、编辑和底部工具入口改为带可访问名称的语义按钮，搜索无结果时显示明确空状态；
- 手机配方详情、材料价格、预计收益、服务器选择、制作数量、价格修改、成本计算器、操作菜单、清单列表及清单详情完成响应式适配，并修复同名样式误伤、路由切换未回顶部及购物车抽屉缺少关闭方法造成的 Vue 警告；
- 旧版 Element Drawer 的 `custom-class` 已改为当前组件支持的 `class`，数量弹层补齐最小制作次数、动态产量上下限、关闭取消和确认归一化；价格弹层、成本计算器及清单抽屉显式修复白底白字、输入值不可见、固定高度空白和触控区域过小问题；
- 移动端预计收益改为“成品单价 × 最终产量 - Σ（材料单价 × 材料数量）”，修复原实现遗漏材料数量和成品产量造成的结果偏差；已有清单编辑会提交新的标题，修复重命名未生效；
- “我的清单”移动抽屉增加标题、加载中、未登录、空数据和请求失败状态，条目使用稳定 ID 和语义按钮，抽屉根据内容自适应高度；读取清单后可进入移动端详情，编辑入口继续复用原成本计算器；
- 移动端新主题显式覆盖旧深色变量，避免系统深色外观下出现白字配浅色卡片的低对比度问题；
- 搜索结果、成本计算器、价格、收益、配方元信息与主要操作已补充简体中文、繁体中文、英文和越南文；游戏配方名、技艺名、材料名和服务器名继续使用源数据；
- 页面样式隔离在 `manufacture-theme.less` 与 `manufacture-mobile-theme.less`，均限定在模块根节点内，不影响未改造页面。

本轮保留原配方、物品合并、区服价格、购物车、材料制作、账单读取、保存、删除和合并接口及权限逻辑，只修正可确认有误的移动端收益公式和已有清单标题提交。Chrome 已回归 1440、820、768、390 宽度，并逐项检查菜单、配方分类、搜索及空状态、详情、制作数量、价格修改、加购、区服选择、成本计算器、操作菜单、清单列表和清单详情；最终 390px 页面宽度与视口一致，控制台无新增错误或警告。目标文件 ESLint、`git diff --check` 和 `npm run build` 已通过，生产构建仅保留项目既有资源体积提示；未执行真实账单保存、删除或合并操作。

价格走势已经完成 Web 端第一轮改造和响应式调整，当前等待用户最终验收，因此 `/pvg/price` TODO 暂不勾选。

本轮价格走势已完成：

- `/pvg/price` 统一使用 `PvxPageShell`、`PvxSurface`、`PvxSectionHeader`、`PvxToolbar` 和 `PvxEmptyState`，总览、金价、物价及关注清单使用同一页面层级；
- 页面级标题、市场快照、金价走势、物价和关注清单统一图标规格，Hero 使用与家园蓝图、活动告示和考试大全一致的克制径向背景；
- 区服选择提升到页面 Hero 右侧并作为三个 Tab 的公共状态，初始化继续兼容用户默认区服和客户端默认值，切换后由各子页面重新请求对应数据；
- 总览市场、金价图表、物价列表、关注清单和清单管理弹窗完成现代卡片、加载与空状态重组，物品跳转继续携带当前区服上下文；
- 图表在容器尺寸变化时通过 `ResizeObserver` 正确重建或调整，组件卸载时释放观察器，避免切换 Tab 和改变视口后的尺寸异常；
- 手机端区服选择占满 Hero 宽度，市场渠道卡片改为一行一项并移除无意义横向滚动；关注清单的“管理清单”放入标题右侧，在手机端收为带可访问名称的紧凑图标按钮；
- 新增价格模块主题集中在 `price-theme.less`，页面覆盖限定在 `.p-pvx-price` 根节点，不影响活动告示、考试大全和未改造页面；
- 补齐简体中文、繁体中文、英文和越南文界面文案；小程序/App 专用价格页面只补充必要国际化和尺寸兼容，不进行与 Web 页面相同的结构重构。

价格走势保留原金价、物价、关注清单接口及权限逻辑。后续最终验收需继续覆盖区服切换、三个 Tab、关键词搜索、关注清单登录态与管理弹窗，以及图表在桌面、平板和手机尺寸下的重绘。

### 5.2 图鉴百科类

- [ ] 奇遇大全 `/adventure`
- [ ] 宠物大全 `/pet`
- [ ] 坐骑大全 `/horse`
- [ ] 家具大全 `/furniture`
- [ ] 声望大全 `/reputation`
- [ ] 书籍大全 `/book`
- [ ] 奇遇珍卷 `/pvx/codex/adventure`
- [ ] 资历宝典 `/pvx/codex/achievement`
- [ ] 剑侠录 `/questsection`
- [ ] 红尘侠影 `/partner`
- [ ] 图鉴百科类模块完成后收敛公共分类、卡片和详情结构

奇遇大全改造约定：

- **范围记忆（2026-07-25）**：后续未特别说明时，“奇遇大全列表/详情”和“奇遇珍卷横屏/竖屏”的改造、国际化检查与回归，均只指普通 Web 页面；`src/views/adventure/miniprogram`、`PvxUserMiniprogram.vue` 及小程序/App 专用路由不纳入本轮范围，不检查、不改造，也不把其存量问题列入本轮缺陷；
- **第三方边界（2026-07-25）**：`@jx3box/jx3box-ui` 等第三方组件的内部样式和国际化不在当前页面改造中处理；即使在奇遇页发现 `WikiRevisions`、`Thx`、`WikiComments` 等组件的内部排版或词条问题，也只记录为第三方组件后续事项，不在本项目内复制、补丁或覆盖其内部实现；
- `/adventure` Web 列表与详情页在 PC 端都必须分别在模块根节点内解除 `PvxPageShell` 的 `1440px` 最大宽度，充分使用可用内容区；
- 全宽只解除内容上限，桌面、平板和手机仍保留 `PvxPageShell` 统一安全边距，不修改公共页面外壳，也不影响小程序/App 页面和其他未改造模块；
- 奇遇竖屏平板概览使用三列弹性网格，分类导航独占首行，搜索框与“奇遇珍卷”位于第二行，避免固定两列造成大块留白或搜索框被压缩；
- 奇遇移动端概览参考 Face / Body 的最小数据量规则，每个分类至少展示 6 项；方形奇遇卡使用两列网格，不能退化为每类只展示一张，也不能产生页面级横向滚动；
- 奇遇 Web 详情页在 PC 端占满可用内容宽度，以卡片面板组织导航、标题、百科攻略、触发记录、历史版本、投币和评论；任务流程画卷保留原有特色视觉和交互，仅在自身容器内横向滚动；
- 奇遇详情中的攻略、触发记录、历史版本和百科评论统一使用同一面板标题规范，包括标题栏高度、主题色图标容器、标题字号、左右内边距和分隔线；不得保留双层卡片嵌套；
- 奇遇详情中的攻略、触发记录、历史版本、投币和百科评论在所有 PC 宽度下都各占独立一行并填满可用内容宽度，不在大屏改为双栏，也不对评论正文另设居中最大宽度；
- 触发记录是项目本地组件，桌面使用四列等宽网格，手机使用带字段标签的两列信息卡；历史版本、投币版权和百科评论来自 `@jx3box/jx3box-ui`，页面只负责把组件作为整体独占一行并填满宽度，保留当前既有表现，后续不再新增针对其内部结构的样式或国际化处理；
- 奇遇详情改造不得影响 QQ 机器人抓图模板、小程序/App 专用详情页，以及低版本游戏内嵌浏览器的既有跳转逻辑；
- 其他图鉴模块是否全宽仍需按内容密度单独确认，不能因为奇遇模块全宽而自动扩大其他页面。

2026-07-24 奇遇大全 Web 端本轮已完成：

- 列表页使用 `PvxPageShell`、`PvxToolbar`、`PvxSurface`、`PvxSectionHeader`、`PvxActionButton` 和 `PvxEmptyState` 重组分类、搜索、珍卷入口、概览分组、分类结果、加载、分页和空状态；移除独立 Hero 说明模块，“奇遇珍卷”与搜索处于同一工具栏；
- PC 列表解除公共 `1440px` 上限并保留安全边距；竖屏平板使用三列网格，手机使用两列网格且每个概览分类至少加载 6 项；中大屏分类结果的加载更多和分页参考 Face 列表规则，不产生页面级横向滚动；
- 概览分组只保留分类标题和“查看全部”，卡片不重复显示图片内已有的奇遇名称；卡片艺术字、上下留白、主题色边框、Hover、按下和键盘焦点状态已经统一，并通过 `modern` 变体隔离旧列表和小程序/App 实例；
- 详情页同样在模块根节点内解除 `1440px` 上限；返回导航、标题信息、成就入口、机器人提示、任务画卷和百科社区区域形成稳定层级，任务流程画卷的原始美术样式与横向切换交互保持不变；
- 攻略、触发记录、历史版本、投币版权和百科评论在所有 PC 宽度下各占独立一行并填满可用宽度；详情标题栏、图标容器、边框、圆角、间距和正文内边距统一，评论正文不再设置二次居中最大宽度；
- `serendipity.vue` 是项目本地触发记录组件，已补齐桌面四列、手机两列字段卡和空状态；`PvxUser.vue` 是本地百科包装组件，新增可选 i18n 前缀，只为奇遇详情启用本地攻略标题、完善入口、兼容提示、修订签名和空状态翻译，其他模块继续使用原回退文案；
- `WikiRevisions`、`Thx` 和 `WikiComments` 来自 `@jx3box/jx3box-ui`，历史版本、投币版权和百科评论继续使用第三方组件自身的数据与国际化能力；当前页面级排布保持不变，不新增第三方内部样式或国际化改动，后续统一在第三方组件专项中优化；
- `PvxRobotTip.vue` 是项目本地公共组件；奇遇详情补齐简体中文、繁体中文、英文和越南文的引导语、复制反馈及可访问名称，指令中的业务类型按产品要求固定为“奇遇”而不做 i18n；QQ 入口继续保持原有默认提示、Hover 切换白底 QQ 号、点击 QQ 号复制的样式和交互；
- 列表与详情新增界面文案已覆盖简体中文、繁体中文、英文和越南文，四套 `pages.adventure` 键结构保持一致；游戏奇遇名、任务正文、服务器名、用户内容、指令业务类型和接口返回数据保持源文，不做错误翻译；
- 新主题集中在 `adventure-list-theme.less` 和 `adventure-detail-theme.less`，分别由列表与详情专有根节点启用；公共组件的新版表现必须由调用方显式传入 `variant` 或 i18n 前缀，不会自动改变其他页面；
- 已完成目标文件 ESLint、`git diff --check` 和多次 `npm run build:dev`；浏览器回归覆盖大屏、常规桌面、iPad 和手机布局，并逐一切换简中、繁中、英文和越南文检查机器人提示，未发现文字遮挡、组件溢出或页面级横向滚动。

奇遇大全当前等待用户最终验收，因此 `/adventure` 主 TODO 暂不勾选。

2026-07-25 奇遇大全 Web 端回归修复：

- 修复经典服详情统计使用未定义变量的问题，`PvxUser.vue` 继续使用现有统计类型，只在经典服增加 `origin_` 前缀；
- 本地触发记录组件的相对时间按当前 `zh-CN`、`zh-TW`、`en-US`、`vi` locale 格式化，不再在英文或越南文界面显示简体中文“几天前”；
- 列表关键词输入增加短延迟合并，请求使用批次序号隔离旧响应；概览三个并发请求全部结束后才关闭加载状态，快速输入、切换分类或加载更多时旧请求不能覆盖新结果；
- `modern` 奇遇卡补齐明确按下态、键盘焦点态，阵营与门派外观切换提供可访问名称和 `44px` 触控区域；异常或缺失的标题图片路径不再导致卡片渲染报错；
- 老版 `/pvx/codex/adventure/treasure` 入口显式跳转到 `/pvx/codex/adventure/landscape`，不再依赖部署服务器二次纠正基路径；
- 按范围约定，本轮没有修改小程序/App 专用页面，也没有修改 `WikiRevisions`、`Thx`、`WikiComments` 的内部样式或国际化；相关第三方组件问题留待其自身专项优化。

2026-07-24 奇遇珍卷第一轮改造约定与进度：

- `/pvx/codex/adventure/landscape` 与 `/pvx/codex/adventure/portrait` 的国风画卷本体、展开动画、数据结构和 QQ 机器人抓图组件保持原样；本轮只改造普通页面的外围外壳、角色工具栏、状态反馈和响应式承载方式；
- 普通 Web 横竖屏页面统一使用全宽 PVX 页面外壳和角色、阵营操作工具栏；不增加独立 Hero、模式切换卡或“画卷预览”标题栏，继续使用原国风背景铺满整个页面，PC 占满可用内容宽度并保留安全边距；横屏工具栏最长与展开后的 2022px 卷轴一致，竖屏工具栏与 940px 画卷宽度对齐；两种模式均移除角色、阵营前置标签，将角色管理与刷新图标分别追加到对应选择器右侧，阵营默认选择浩气盟；竖屏保持旧版只有“刷新画卷、角色管理”的操作边界，不额外展示同步数据或保存图片入口；
- 横屏画卷在平板和窄桌面只允许预览面板内部横向滚动，页面本身不得出现横向滚动；竖屏画卷按预览容器宽度自动缩放，390px 手机和 768px 竖屏平板均保持完整宽度；
- 普通 Web 竖屏入口补齐全局头部与页脚，小程序/App 环境继续保持独立嵌入模式，不重复渲染全局导航；
- 登录、无角色、角色加载、数据加载和刷新均提供明确状态；刷新按钮防止重复提交，横屏图片导出的缩放恢复放入 `finally`，定时器和窗口事件在组件卸载时清理；
- 珍卷外围界面补齐简体中文、繁体中文、英文和越南文，四套 `pages.adventure.treasure.ui` 键结构保持一致；奇遇名称和画卷中的原始游戏内容继续使用源数据；
- 横竖屏画卷的进度、记录时间、无记录状态、标点和日期时间格式均跟随当前语言；QQ 机器人抓图入口未传语言参数时继续使用原有简体中文默认值；
- 角色工具栏提取为本地 `TreasureControlBar.vue`，新版样式集中在 `treasure-shell-theme.less` 并限定于 `.p-pvx-treasure`，不影响 QQ 机器人抓图模板及其他模块。

2026-07-25 奇遇珍卷交互补充：

- 横屏大屏工具栏的角色、阵营组合控件总宽度由 `640px` 收窄至 `540px`，工具栏整体仍与 2022px 画卷等宽；`1133px` 及以下继续按既有响应式规则占满工具栏可用宽度；
- 普通 Web 画卷中的普通奇遇整项、绝世奇遇艺术文字和宠物奇遇整项均可进入对应 `/adventure/:id` 详情；
- 详情跳转与奇遇列表统一：桌面端使用新窗口打开以保留筛选、滚动、角色、阵营和画卷展开状态，移动端在当前窗口打开；桌面外链补充 `noopener noreferrer`；
- 绝世奇遇艺术文字提供 Hover、键盘焦点和按下反馈，宠物奇遇 Hover 或键盘聚焦时显示接口返回的原始奇遇名称 Tooltip；奇遇名称属于游戏源数据，不进行 i18n；
- 以上交互通过默认关闭的 `enableDetailLinks` 属性启用，只在普通 Web 入口开启；QQ 机器人抓图不传该属性，竖屏入口在小程序/App 环境显式关闭，因此相关终端保持原样；
- 本次交互补充已通过目标文件 ESLint、`npm run verify:legacy-js`、`npm run build:dev` 和 `git diff --check`；按用户要求未进行浏览器测试，由用户完成横竖屏页面验收。

2026-07-24 第一轮改造已通过目标文件 ESLint、`npm run verify:legacy-js`、`npm run build:dev` 和 `git diff --check`。当时的 Chrome 登录态回归覆盖 1280px 桌面、1024px 横屏平板、768px 竖屏平板与 390px 手机；未触发刷新、同步和图片下载等产生任务或文件的操作。

### 5.3 公共区域与特殊入口

- [ ] 公共头部视觉统一
- [ ] 公共页脚视觉统一
- [ ] `Nav_v5` 展开、收起、激活和滚动状态统一
- [ ] 公共搜索、筛选、分页、空状态和加载状态统一
- [ ] 小程序和 App 页面按对应终端规范改造
- [ ] 全部业务模块完成后清理 `legacy` 兼容层

## 6. 单模块实施流程

每个后续模块按以下顺序执行，并在用户验收后再进入下一模块：

1. 阅读路由、入口页面、子页面、业务组件、接口和现有样式。
2. 阅读视觉设计规范，对照已验收页面梳理可复用能力。
3. 列出必须保留的业务差异、接口行为和终端差异。
4. 建立模块专有根节点，确保样式不会泄漏到其他页面。
5. 先完成桌面端核心结构，再补齐平板与移动端。
6. 检查多语言长文本、空数据、加载、错误和权限状态。
7. 回归接口参数、购买、下载、跳转、统计等原有功能。
8. 运行自动检查，并按设计规范中的建议视口进行浏览器验收。
9. 用户验收通过后更新本 TODO；同类模块稳定后再抽取公共能力。

## 7. 业务验收标准

- 接口请求数量和参数符合原逻辑。
- 登录、作者、管理员和付费权限判断不变。
- 购买确认、支付轮询、余额不足、成功和失败提示正常。
- 下载入口、附件列表、单文件下载和批量下载正常。
- 路由跳转、外链、统计和评论等能力正常。
- 第三方组件的数据解析、上传和国际化行为不变。
- 小程序和 App 端未进入改造范围时不受影响。

视觉验收使用 `docs/design/pvx-visual-style-guide.md` 中的检查清单。

## 8. 代码质量与自动检查

- 公共 props 和 slots 语义清晰，没有为单页场景过度抽象。
- 没有重复包装第三方组件的数据解析或国际化。
- 没有业务无关的截图、临时文档或调试文件进入 Git。
- 新增公共能力后删除已被替代的重复代码。
- 未改造页面的旧代码不因当前任务被顺带重写。

固定自动检查：

```bash
npm run lint
npm run verify:legacy-js
npm run build:dev
```

## 9. 文档维护规则

- 本文只更新模块状态、实施顺序、业务风险和验收结论。
- 视觉规范变化只更新 `docs/design/pvx-visual-style-guide.md`。
- 新视觉模式先在业务页面落地并验收，确认稳定复用后再写入设计规范。
- 不保留逐日流水账和已完成步骤的冗长过程记录。
