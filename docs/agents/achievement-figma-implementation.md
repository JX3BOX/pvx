# 资历宝典 Figma 样式实施

设计来源：`pJqKnVS6RqMlmtizpnJIDq`，2026-09-08。用户已授权按截图范围逐页实施并统一验收。

采用浅色画板：完成进度 `74:1479` / `83:18603`，亲友对比 `96:26009` / `149:11495`，渡劫方案 `99:7926` / `138:13635`，方案列表 `149:15431`，使用指南 `123:9743`。链接中的 `1:245` 为早期深色稿，不用于本次实现。

## 约束

- Vue 3 Options API、现有 LESS 和 Element Plus；不增加 UI 依赖。
- 保留接口、筛选、推荐算法、角色权限、国际化、真实成就与方案数据。
- 设计变量：青灰 `#5A7E84`、暖金 `#967944`、深金 `#6E572C`、浅底 `#F8F7F3`、白色 `#FFF`、正文 `#333`。
- 桌面外边距 24px、模块间距及内边距 12px；指南最大宽度 1440px。
- 正文保留至少 14px、辅助信息至少 13px，移动端控件保留 44px 点击区域。
- 图标与竹叶从 Figma 导出并存入 `src/assets/img/wiki/figma`；角色与成就图片继续取真实业务数据。
- 使用指南保留游戏内同步步骤和截图，采用折叠展示以保持设计稿的阅读密度。

## 实施顺序

- [x] 公共外壳、导航与设计素材。
- [x] 完成进度：头像环、展开与紧凑统计、双列分类和成就详情。
- [x] 亲友对比：四栏角色卡、添加入口、分类树、状态矩阵。
- [x] 渡劫方案：基础表单、分类卡、偏好选项、推荐按钮、方案侧栏。
- [x] 使用指南：返回入口、标题卡、步骤卡、功能与常见问题。
- [x] 成就回归脚本、构建、桌面及窄屏集中视觉检查。

## 保留的业务差异

- 方案名称仍允许 40 字，不将设计稿示例中的 20 字变成新的业务限制。
- 维度名称、选项含义、成就图标及统计值取现有数据；维度较多时允许换行。
- 渡劫咨询入口继续按原有权限展示，指南仍包含网站绑定、游戏内同步、回站检查三个完整步骤。
- 推荐结果、方案保存、角色切换、筛选和分页沿用现有业务实现；偏好和分类支持展开/收起。
- 桌面三人状态保留第四格添加入口，四人时显示四张角色卡；手机矩阵在表格内横向滚动。

## 验证记录

- `node scripts/test-achievement.js`：12 组全部通过，包含角色切换、推荐请求与保存、普通成就口径和四语言契约。
- `node node_modules/@vue/cli-service/bin/vue-cli-service.js build --mode production`：成功。构建仍报告 CSS 提取顺序、部分其他页面的 calc 表达式及资源体积警告；没有编译错误。
- `git diff --check`：通过。
- 使用真实组件和隔离的本地样例数据检查桌面展开/收起总览、三人/四人对比、方案偏好与抽屉、指南；390px 窄屏检查无页面横向溢出，指南图示加载正常，方案抽屉为单列。
- 800px 宽度下导航高 120px，推荐工具栏 top 为 180px，与公共头部及双行导航对应。
- 视觉检查使用样例角色，未替代真实登录账号的在线接口验收。
